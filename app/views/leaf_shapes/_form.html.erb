<%= form_for(@leaf_shape) do |f| %>
  <% if @leaf_shape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leaf_shape.errors.count, "error") %> prohibited this leaf_shape from being saved:</h2>

      <ul>
      <% @leaf_shape.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
