<%= form_for(@plant_bloom_color) do |f| %>
  <% if @plant_bloom_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plant_bloom_color.errors.count, "error") %> prohibited this plant_bloom_color from being saved:</h2>

      <ul>
      <% @plant_bloom_color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plant_id %><br>
    <%= f.number_field :plant_id %>
  </div>
  <div class="field">
    <%= f.label :bloom_color_id %><br>
    <%= f.number_field :bloom_color_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
