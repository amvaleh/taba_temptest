<%= form_for(@medical_feature) do |f| %>
  <% if @medical_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_feature.errors.count, "error") %> prohibited this medical_feature from being saved:</h2>

      <ul>
      <% @medical_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
