<%= form_for(@physical_structure) do |f| %>
  <% if @physical_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@physical_structure.errors.count, "error") %> prohibited this physical_structure from being saved:</h2>

      <ul>
      <% @physical_structure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :avatar %><br>
        <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
