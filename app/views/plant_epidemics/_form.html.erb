<%= form_for(@plant_epidemic) do |f| %>
  <% if @plant_epidemic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plant_epidemic.errors.count, "error") %> prohibited this plant_epidemic from being saved:</h2>

      <ul>
      <% @plant_epidemic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plant_id %><br>
    <%= f.number_field :plant_id %>
  </div>
  <div class="field">
    <%= f.label :epidemic_id %><br>
    <%= f.number_field :epidemic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
